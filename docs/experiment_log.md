# Experiment Log

| Date | Experiment | Hypothesis / Objective | Configuration | Results (metrics) | Interpretation |
|------|-----------|----------------------|---------------|-------------------|----------------|
| ~2026-02 | supervised_15_legacy | Reproduce legacy parquet-based baseline in static directory setup | DirectClassifier, d=128, L=4, ctx=32, bs=256, lr=3e-3, 12ep, legacy parquet data | ~80% top-1 | Baseline reproduced; confirms legacy dataset and pipeline are sound |
| ~2026-02 | supervised_16_log1p_znorm | Normalization (log1p + ZNorm) explains accuracy gap between legacy and new memmap pipeline | DirectClassifier, d=128, L=4, ctx=32, log1p+ZNorm, matched LR/ds_limit | ~70% top-1 | Normalization is not the cause of the regression |
| ~2026-02 | supervised_17_fwd_kin_only | Using fi/fp for both strands (legacy behavior) sidesteps reverse kinetics issue | DirectClassifier, d=128, L=4, ctx=32, fi/fp only (no ri/rp) | ~80% top-1 | Confirms reverse kinetics handling is the problem; fi/fp-only matches legacy |
| ~2026-02 | supervised_18_v2_clean_shards | v2 memmap script with fixed reverse indexing resolves the regression | DirectClassifier, d=128, L=4, ctx=32, v2 memmaps | ~80% top-1 | v2 fix confirmed; reverse kinetics misalignment was the root cause |
| ~2026-02 | supervised_20_full_v2 | Full production run on v2 memmaps with all available data | DirectClassifier, d=128, L=4, H=4, ctx=32, bs=512, lr=3e-3, 20ep, ds_limit=0, bf16 | ~82% top-1 | Supervised baseline established at ~82% on full dataset |
| ~2026-03 | ssl_21_pretrain | Contrastive pretraining (InputMask + InfoNCE) produces transferable representations | Smrt2VecInputMask, d=128, L=4, H=4, ctx=4096, bs=64, lr=3e-4, p_mask=0.15, mask_size=10, temp=0.1, max_neg=8192, 12ep, AgInfoNCE | InfoNCE loss converges; probe accuracy declines over epochs | SSL training works but representations don't transfer to CpG classification. Three blockers: norm mismatch, length mismatch, task misalignment |
| ~2026-03 | ssl_23_shortctx | Reducing pretraining context (128 vs 4096) closes length gap with probe (ctx=32) | Same as exp 21 but ctx=128, bs=512, max_neg=1024 | Probe accuracy still declines | Length mismatch was not the primary blocker; task misalignment is likely dominant |
| ~2026-03 | ssl_24_autoencoder | Direct kinetics reconstruction (MSE) forces encoder to preserve methylation-relevant signal | SmrtAutoencoder, d=128, L=4, H=4, ctx=128, bs=512, lr=3e-4, p_mask=0.15, mask_size=10, MaskedReconstructionLoss, 12ep | Probe accuracy ~62-63% top-1 | Marginal improvement over contrastive (~58%). Switching from InfoNCE to direct reconstruction helps slightly but does not close the gap to the supervised baseline (~80%). The pretraining objective is not the sole bottleneck — architectural or data-level issues likely contribute |
| 2026-03-30 | ssl_25_cpg_autoencoder | Eliminating data distribution mismatch (CpG data, ctx=32) will improve autoencoder probe accuracy | SmrtAutoencoder, d=128, L=4, H=4, ctx=32, bs=1024, lr=3e-4, p_mask=0.15, mask_size=5, MaskedReconstructionLoss, 12ep, CpG pos+neg data (labels discarded), KineticsNorm from CpG data | Probe ~66% top-1, stable across epochs. ~3k steps, 30 min on 8 GPUs | +4pp over exp 24 (full-read autoencoder). CpG data regime helps but doesn't close the gap to supervised 82%. Autoencoder still outperforms contrastive by ~3pp. Probe accuracy plateaued — more epochs unlikely to help |
| 2026-03-30 | ssl_26_cpg_contrastive | Head-to-head: does contrastive also improve on CpG data, or is autoencoder inherently better? | Smrt2VecInputMask, d=128, L=4, H=4, ctx=32, bs=1024, lr=3e-4, p_mask=0.15, mask_size=5, AgInfoNCE temp=0.1 max_neg=256, 12ep, CpG pos+neg data (labels discarded), KineticsNorm from CpG data | Probe ~63% top-1, stable across epochs. ~3k steps, 30 min on 8 GPUs | +5pp over exp 21/23 (full-read contrastive). CpG data regime provides similar improvement (~4-5pp) to both architectures. Contrastive still ~3pp below autoencoder on matched data |
| 2026-03-30 | supervised_27_finetune_ae | Full fine-tuning with exp 25 encoder unlocks non-linear signal that linear probing misses | DirectClassifier + exp 25 encoder, d=128, L=4, H=4, ctx=32, bs=512, ds_limit=20M, KineticsNorm from CpG data, Stage 1: frozen 5ep lr=3e-3, Stage 2: unfreeze 15ep encoder_lr=3e-4 head_lr=3e-3, 2hr on 8 GPUs | 79% top-1 | Fine-tuning recovered 13pp over probe (66→79%) but still 3pp below supervised from scratch (82%). Comparison is confounded: different optimizer schedules (two-stage differential LR vs single-stage), and pretraining used the same CpG data with labels removed — no information advantage from SSL. The 10x lower encoder LR (3e-4 vs 3e-3) during fine-tuning may be undertrained |
| 2026-03-31 | supervised_28_baseline_control | Disambiguate whether 3pp gap is from fine-tuning schedule or pretraining hurting | DirectClassifier from scratch, d=128, L=4, H=4, ctx=32, bs=512, lr=3e-3, 20ep, ds_limit=20M (matching exp 27 data budget) | [pending] | [pending] |
| 2026-03-31 | ssl_29_large_pretrain | Large-scale SSL tests actual value proposition: more unlabeled data than labeled | SmrtAutoencoder, d=128, L=4, H=4, ctx=128, bs=512, lr=3e-4, p_mask=0.15, mask_size=10, 3000ep, ds_limit=0 (all 839K reads), random cropping from 4096→128, pct_start=0.01, checkpoint every 100ep, probe every 100ep, ~1000 GPU hours. Ran on 25G subset of OB007 | 63% probe top-1 | Pretraining on 25G OB007 subset produced 63% probe accuracy — comparable to exp 25 (66%) on CpG data. Full OB007 dataset pending |
| 2026-04-07 | ssl_30_smallrf_autoencoder | Architectural fix: reducing CNN receptive field from 107 to 27 restores per-latent locality at ctx=32 and closes the 3pp gap between exp 27 fine-tune (79%) and exp 20 supervised baseline (82%) | SmrtAutoencoderSmallRF (4-block CNN: k=3, k=3, k=3 s=2, k=3 s=2; r0=27; 4x downsample preserved), d=128, L=4, H=4, ctx=32, bs=1024, lr=3e-4, p_mask=0.15, mask_size=5, 12ep, ds_limit=2M, CpG pos+neg data (labels discarded), KineticsNorm from CpG data — identical to exp 25 except for the encoder class. 1.35M params vs exp 25's 2.17M (−823k from removing 7 ResBlocks) | [pending] | [pending] |
| 2026-04-10 | supervised_33_data_scaling | How does CpG classification performance scale with training data volume? Establishes a data scaling curve for the supervised baseline | DirectClassifier d=128, L=4, H=4, ctx=32, bs=512, lr=3e-3, wd=0.02, pct_start=0.1, 200k steps per size (fixed budget, not fixed epochs), train_sizes=[100,500,1k,2k,4k,8k,16k,32k,64k,128k], val=1M fixed, 10 evals per size, 1 GPU, bf16, seed=42, fresh model+norm per size | [pending] | [pending] |
| 2026-04-13 | supervised_34_data_scaling_pretrained | Does a pretrained encoder (exp 25) improve the data scaling curve vs random init (exp 33)? Directly comparable scaling curves | DirectClassifier with exp 25 encoder, d=128, L=4, H=4, ctx=32, bs=512, two-stage: 100k frozen (head lr=3e-3) + 300k unfrozen (encoder lr=3e-4, head lr=3e-3), wd=0.02, pct_start=0.1, 400k steps per size, same 16 train_sizes as exp 33, val=1M fixed, 20 log-spaced evals, 8 GPU, bf16, seed=42 | [pending] | [pending] |
| 2026-04-14 | supervised_35_data_scaling_ob007 | Does exp 29 OB007 pretrained encoder improve the data scaling curve vs random init (exp 33)? | DirectClassifier with exp 29 encoder (25G OB007, 63% probe), d=128, L=4, H=4, ctx=32, bs=512, two-stage: 100k frozen (head lr=3e-3) + 300k unfrozen (encoder lr=3e-4, head lr=3e-3), wd=0.02, pct_start=0.1, 400k steps per size, same 16 train_sizes as exp 33, val=1M fixed, 20 log-spaced evals, 8 GPU, bf16, seed=42. PE buffer reinit due to ctx 128→32 mismatch | [pending] | [pending] |
| 2026-04-14 | supervised_36_pretrained_matched_schedule | Controls for schedule confound in exp 34: exp 25 pretrained encoder with exp 33's exact training procedure (single AdamW lr=3e-3, single cosine 400k steps). Only difference from exp 33 is encoder init | DirectClassifier with exp 25 encoder, d=128, L=4, H=4, ctx=32, bs=512, single-stage: AdamW lr=3e-3 all params, cosine 400k steps, wd=0.02, pct_start=0.1, same 16 train_sizes as exp 33, val=1M fixed, 20 log-spaced evals, 8 GPU, bf16, seed=42 | [pending] | [pending] |
| 2026-04-14 | supervised_37_ob007_matched_schedule | Controls for schedule confound in exp 35: exp 29 OB007 pretrained encoder with exp 33's exact training procedure. Only difference from exp 33 is encoder init | DirectClassifier with exp 29 encoder (25G OB007, 63% probe), d=128, L=4, H=4, ctx=32, bs=512, single-stage: AdamW lr=3e-3 all params, cosine 400k steps, wd=0.02, pct_start=0.1, same 16 train_sizes as exp 33, val=1M fixed, 20 log-spaced evals, 8 GPU, bf16, seed=42. PE reinit ctx 128→32 | [pending] | [pending] |
| 2026-04-14 | supervised_38_ob007_gradual_unfreeze | Does gradual encoder unfreezing (1e-5 → 3e-4 over 100k steps) improve fine-tuning vs exp 35's instant jump to 3e-4? | DirectClassifier with exp 29 encoder, two-stage: 100k frozen (head lr=3e-3) + 300k unfrozen (encoder 1e-5→3e-4 over 100k warmup, head 3e-3), per-group LambdaLR, 40 log-spaced evals (2x exp 35), same 16 train_sizes, val=1M, 8 GPU, bf16, seed=42 | [pending] | [pending] |
| 2026-04-16 | supervised_40_baseline_v2 | Fix LR scheduler bug from exp 31: accelerator.prepare(scheduler) caused pct_start=0.1 to not produce peak at 10% of training. Clean code rewrite with identical architecture/data/optimizer | DirectClassifier d=128, L=4, H=4, ctx=32, bs=512, lr=3e-3, wd=0.02, pct_start=0.1, 20ep, ds_limit=0, bf16, 8 GPU DDP, seed=42. Scheduler created directly on prepared optimizer, stepped manually (no AcceleratedScheduler wrapping) | [pending] | [pending] |
| 2026-04-16 | supervised_41_baseline_epoch_cap | Rerun exp 33/39 with epoch-capped step budget to prevent catastrophic overtraining at small dataset sizes. Exp 33 trained n=100 for 400k epochs; this version caps at min(max(200*steps_per_epoch, 10k), 400k) per size | DirectClassifier d=128, L=4, H=4, ctx=32, bs=4096, lr=3e-3, wd=0.02, pct_start=0.1, max_epochs=200, min_steps=10k, max_steps=400k, same 16 train_sizes, val=1M, 40 evals, 1 GPU per size, bf16, torch.compile, seed=42 | [pending] | [pending] |
| 2026-04-16 | supervised_42_ob007_matched_epoch_cap | OB007 pretrained (exp 29) with exp 41's exact epoch-capped schedule. Only difference from exp 41 is encoder init | Same as exp 41 (bs=4096, torch.compile) but with exp 29 pretrained encoder. Single-stage AdamW lr=3e-3 all params | [pending] | [pending] |
| 2026-04-16 | supervised_43_ob007_gradual_epoch_cap | OB007 pretrained (exp 29) with two-stage gradual unfreeze and epoch-capped budgets. Proportional scaling: 25% frozen, encoder warmup over 1/3 of stage 2 | Same as exp 41 step budget (bs=4096, torch.compile). Two-stage: frozen_frac=0.25, then encoder 1e-5→3e-4 over warmup_frac=0.333 of stage 2, head lr=3e-3. All fractions relative to per-size total_steps | [pending] | [pending] |
| 2026-04-17 | ssl_50_simclr_pilot | Pilot for SimCLR v1 direction. Does augmentation-based SSL with SimCLRSmrt + NT-Xent transfer at all in this domain without the declining-probe failure that killed exps 21/23? Pretrain on full-read OB007 subcropped to ctx=32; evaluate with the existing linear-probe harness on CpG pos/neg every 5 epochs. Pass criterion: probe_top1 ≥ 0.63 at end AND non-decreasing over last 3 evals. | SimCLRSmrt (SmrtEncoder + 2-layer MLP projection head): d=128, L=4, n_head=4, ctx=32, projection_dim=128. NT-Xent temp=0.1, max_negatives=None (full global pool). AugmentationPolicy: random_subcrop always, revcomp_p=0.0, channel_dropout_p=0.2, gaussian_noise_p=0.8 σ=0.1, blur_p=0.5 σ∈[0.2,2.0]. 100 epochs × ~200k OB007 reads at bs=512 per-rank × 8 GPUs (global=4096). AdamW lr=3e-4 wd=1e-4, cosine schedule pct_start=0.05. KineticsNorm dataset-side. | [pending] | [pending] |
| 2026-04-17 | ssl_51_simclr_grid_r1 | Round-1 scoping grid for SimCLR model scaling. Does probe_top1 increase monotonically with model size under the pilot's augmentation policy? Four sizes span 2.1M to 102M params; same data, step budget, and augmentation as ssl_50. Each size is an independent SLURM submission sized under the current ~400 H100-hour per-request cap. Result: per-size end-of-training probe_top1 and the scaling slope across sizes; a flat or inverted slope kills Round 2 pending augmentation revisit. | Four configs in ssl_51_simclr_grid_r1/size_{d128_L4,d256_L8,d512_L8,d768_L8}. Each config mirrors ssl_50_simclr_pilot except for (d_model, n_layers, n_head) = (128,4,2) / (256,8,4) / (512,8,8) / (768,8,12) giving measured param counts (2.1M, 11.4M, 45.5M, 102.2M) and batch_size=512 except d768 at bs=256 for memory. head_dim=64 fixed. 100 epochs × 200k reads, AdamW lr=3e-4, cosine pct_start=0.05. Linear probe every 5 epochs on the exp-25/26 CpG val set. | [pending] | [pending] |
| 2026-04-20 | supervised_44_ob007_gradual_scaled_bs | Does per-size batch scaling fix small-n overfitting seen in exp 43? At n_train≤bs in exp 43 every batch was essentially the full set with replacement — deterministic descent into memorisation. Scale bs with data to restore SGD noise at small n while preserving exp 43's large-size throughput. | Same as exp 43 (OB007 pretrained, gradual unfreeze, epoch-capped budget, frozen_frac=0.25, encoder 1e-5→3e-4 over warmup_frac=0.333, head lr=3e-3, wd=0.02) except train_bs = min(4096, max(64, n_train // 8)) computed per size. Eval bs=4096. Same 16 train_sizes, val=1M, 40 evals, 8 GPU, bf16, torch.compile, seed=42. Walltime 4h. | [pending] | [pending] |
| 2026-04-20 | supervised_45_ob007_gradual_small_bs | Alternative to exp 44: does a globally small batch (bs=64) — strictly below the smallest train_size — eliminate small-n overfitting while keeping the optimisation regime identical across sizes? Simpler than per-size scaling but wastes throughput at the large end. | Same as exp 43 but batch_size=64 globally and LRs scaled by sqrt(64/4096)=1/8: frozen_lr=head_lr=4e-4, encoder_lr=4e-5, encoder_start_lr=1e-6. All other knobs identical (gradual unfreeze, frozen_frac=0.25, warmup_frac=0.333, wd=0.02, pct_start=0.1, epoch cap max_epochs=200/min_steps=10k/max_steps=400k). 16 train_sizes, val=1M, 40 evals, 8 GPU, bf16, torch.compile, seed=42. Walltime 4h. | [pending] | [pending] |
| 2026-04-21 | supervised_46_ob007_matched_scaled_bs | Exps 43/44/45 fixed small-n overfitting but regressed top-1 val accuracy at large n vs exp 42, because the gradual-unfreeze schedule caps encoder_lr at 3e-4 (10× below exp 42's 3e-3) and wastes 25% of the budget in a head-only frozen phase. Isolate the bs-scaling fix: does exp 42's single-stage schedule + per-size batch sizing recover large-n top-1 accuracy while preserving the small-n improvements of 43/44/45? | Exp 42 config unchanged (single-stage AdamW max_lr=3e-3 for ALL params, cosine with pct_start=0.1, wd=0.02, OB007 pretrained encoder from exp 29) plus exp 44's compute_batch_size helper: train_bs = min(4096, max(64, n_train // 8)). Eval bs=4096. Same 16 train_sizes, val=1M, 40 evals, max_epochs=200/min_steps=10k/max_steps=400k (matches exp 42, not the doubled budget of 43/44), 8 GPU, bf16, torch.compile, seed=42. Walltime 4h. | [pending] | [pending] |
| 2026-04-21 | profile_ssl51_compute_bound | Diagnostic: attribute SSL-51 SimCLR per-step wall time to I/O vs forward vs NTXent all_gather vs backward all_reduce vs optimizer for all four grid sizes. Tests the prior belief (memory/feedback_throughput_estimates.md) that small-ctx SSL is dataloader-bound, and the new concern that NTXent's differentiable `dist_nn.all_gather` on z1/z2 (loss.py:100-107) is eating visible wall time on a full pod. | scripts/profile_ssl51_compute_bound.py on 8 H100s, fp32 (matches --mixed_precision=no). Progressive phase ladder per size: A (prepared DL only) → B1 (fwd cached) → B2 (+NTXent) → B3 (+backward) → B4 (+opt = step_c) → B5 (step with real dl). 5 warmup + 20 timed steps per phase, p50/p95 per rank, cross-rank mean. Sizes profiled largest-first (d768, d512, d256, d128) to avoid allocator fragmentation. ds=ob007_raw.memmap, ctx=32, bs matches config.yaml per size (256 for d768, 512 else). walltime=30 min, ~12 min expected. | p50 step_c / step_r (ms): d768 80.2/104.9, d512 79.2/102.0, d256 36.0/67.4, d128 17.7/72.7. all_gather fraction flat at 0.7-4.1% (0.5-0.7 ms absolute). bwd+grad-comm flat at 67-72% across 48× parameter range. Real-loop DL penalty (B5-B4)/B4: +31% / +29% / +87% / +311%. p95 on phase A: 1.2-19 ms median with 1194-4226 ms tails (~1 bad batch per 20). | NTXent all_gather and DDP grad all_reduce are not bottlenecks — AG is a fixed ~0.5 ms launch-latency floor, and bwd% would grow with param count if grad comms were saturating (it doesn't, it gets slightly smaller). The actual story is dataloader stalls: p95 multi-second tails on phase A indicate intermittent worker stalls that drain the prefetch queue, leaving the GPU idle up to 75% of the time at d128. Follow-up: grid sweep (num_workers, prefetch_factor, persistent_workers) to minimize B5-B4. |
| 2026-04-21 | profile_ssl51_dataloader_grid | Follow-up to profile_ssl51_compute_bound: sweep DataLoader config to minimize the real-loop dataloader penalty (B5-B4). Tests whether num_workers / prefetch_factor / persistent_workers tuning can recover the GPU idle time observed at d128 (+311%) and d256 (+87%). | scripts/profile_ssl51_dataloader_grid.py on 8 H100s, fp32. Grid of 11 DL configs × 2 representative sizes (d128_L4 most DL-sensitive, d768_L8 most compute-bound). For each: phase A (pure IO) + phase B5 (real step), 5 warmup + 20 timed. num_workers ∈ {2,4,8,16}, prefetch_factor ∈ {2,4,8}, persistent_workers tested True vs False at baseline. walltime=30 min, ~10 min expected. | d128 rank-1 config (4, 4, True) at B5_p50=35.99 ms (28 it/s projected); d768 rank-1 same config at B5_p50=88.05 ms. persistent_workers=False showed anomalous low p95 at d768 but was flagged as likely n=20 sampling noise. | CPU oversubscription identified: 8 workers/rank × 8 ranks = 64 processes + 8 training processes > 64-core node. Shipped num_workers=8→4 into _shared_train.py. BUT: short-burst profile was wrong — real d128 training came in at 1.59 it/s sustained (not 28), because the 20-iteration measurement pops from a pre-warmed prefetch queue and can't see sustained pipeline cost. Follow-up: IO intervention sweep below. |
| 2026-04-23 | profile_ssl51_io_interventions | Root-cause the ~17× sustained-throughput gap (1.6 real vs 28 predicted) at d128. Two hypotheses: (a) dataset __getitem__ reads 36 KB/sample (4096 × 9 uint8) when AugmentationPolicy.random_subcrop keeps only 32 positions = 128× over-read; (b) random-access disk seeks hurt despite node memory being only 4% used. Primary intervention: SubcropShardedMemmapDataset with read_len ∈ {4096, 256, 64, 32} to cut bytes-per-sample. Falsification check: ChunkedRandomSampler at read_len=4096 — if it alone helps, hypothesis (a) is wrong. | scripts/profile_ssl51_dataloader_grid.py (overwritten — prior CPU-scheduling content replaced). 6 cells at d128_L4 only, num_workers=4, prefetch=4, persistent=True held constant. Per cell: 30 warmup + 20 pre-time + 60s-targeted sustained measurement on phase A (pure next(it)) and B5 (real training step), 4 sub-windows for drift detection. SubcropShardedMemmapDataset defined inline (not yet promoted to smrt_foundation.dataset). Baseline cell routes through the real ShardedMemmapDataset to reproduce production code path. walltime=01:00:00, ~25 min expected. | B5 sustained it/s: baseline (shuffle, r=4096) 1.46, shuffle+r=32 1.77 (+1.22×), chunked+r=4096 5.33 (+3.66×), chunked+r=64 8.53 (+5.86×). All cells flat-drift (no page-cache thrashing). | Primary hypothesis was WRONG: subcrop alone only gives 1.22× despite 128× byte reduction. ChunkedRandomSampler at full read_len (sampler-only change) gives 3.66× — random disk seeks / page-cache-page-fault behaviour on the large memmap is the real bottleneck. Bytes-per-sample matters only secondarily (~1.6× on top of chunked). Ship ChunkedRandomSampler into future training scripts (ssl_53); subcrop deferred until SSL-quality trade-off is tested. |
| 2026-04-23 | ssl_53_simclr_grid_r2_step | Round 2 of the SimCLR grid. Exp 52 showed the larger models beginning to overfit under the 20M-read × 100-epoch budget; R2 moves to ds_limit=0 (~100M reads per epoch, ~5× more unique samples before the model re-sees data) and switches probe + milestone checkpoints + resume state to step-based cadences (every 10k steps) so metrics aren't undersampled at the large end of the grid and crash loss is bounded regardless of epoch length. Throughput feasibility comes from shipping the ChunkedRandomSampler fix from profile_ssl51_io_interventions. | 4 sizes (d128_L4, d256_L8, d512_L8, d768_L8) mirror ssl_52 except: ds_limit=0, epochs=50 (cosine horizon over ~48 h walltime), walltime=48:00:00, DataLoader uses ChunkedRandomSampler(chunk_size=2048, shuffle_within=True) in place of shuffle=True, probe_every_steps=10000, checkpoint_every_steps=10000, resume_every_steps=10000 (step-based Accelerate state save; mid-epoch resume via accelerate.skip_first_batches, ProgressState.step_in_epoch tracked). Portable milestone renamed step_{global_step}.pt and now stores encoder_state_dict only (SimCLR paper §4.2: projection head discarded downstream — halves file size and removes "which key do I load" ambiguity). Encoder load recipe documented inline in _shared_train.py. All other simclr/augment/probe hyperparameters unchanged from ssl_52 (AdamW lr=3e-4 wd=1e-4 pct_start=0.05 grad_clip=5.0 temperature=0.1). | [pending] | [pending] |
| 2026-04-24 | supervised_48_simclr_r2_d256_L8 | First downstream transfer test for the ssl_53 SimCLR Round-2 encoders. Does the d256_L8 SimCLR checkpoint beat the random-init scaling curve (exp 47) when fine-tuned on CpG methylation classification, and if so where on the size grid is the gap largest? Larger encoder (~11M params, first d256/L8 downstream test in this project) means the comparison to the d128/L4 autoencoder lineage (exps 42–46) is confounded by arch size as well as SSL objective — but matching exp 47's schedule exactly isolates the "encoder init" factor against the matching random-init baseline. | DirectClassifier d=256, L=8, H=4, ctx=32 to match the SimCLR encoder. Single-stage AdamW max_lr=3e-3, wd=0.02, cosine pct_start=0.1 (no frozen head phase — `ds_grid.py` falls through to single-stage when `finetune:` key is absent). Per-size batch sizing `min(4096, max(64, n_train // 8))` (exp 46 formula). Step budget `min(max(200*steps_per_epoch, 10k), 400k)` identical to exps 41/46/47. 16 train sizes 100→8M, val=1M fixed, 40 log-spaced evals, 1 GPU per size (`num_processes: 1`, `mp.spawn` across 8 H100s via greedy load-balanced size assignment), bf16, torch.compile, seed=42. Pretrained encoder loaded from `ssl_53_simclr_grid_r2_step/size_d256_L8/checkpoints/final_model.pt` (PE key filtered on shape mismatch per ds_grid.load_pretrained_encoder; shapes match here since both use ctx=32). Walltime 8h. | [pending] | [pending] |
| 2026-04-24 | supervised_49_simclr_r2_d256_L8_persize | Exp 48 redux with per-size schedule control. Exp 48's val_loss at n=8000 bottomed at step 289 of 10000 (~3% through the run) and climbed monotonically to 4.28 by run end; root cause traced to `scaling.min_steps: 10000` silently overriding `max_epochs: 200` for every size where `max_epochs × steps_per_epoch < 10000` (i.e. all n ≤ 32000). New `size_overrides` mechanism in `scripts/ds_grid.py` lets each `train_size` override `max_lr` / `max_epochs` / any other `classifier` or `scaling` knob. Hypothesis: shifting the small-n step budget to the nominal cap plus lowering max_lr in the overfitting-prone bucket pushes val_loss minimum to near 50% of the run. | DirectClassifier d=256, L=8, H=4, ctx=32 (identical to exp 48). `scaling.min_steps` dropped 10000→100 globally so `max_epochs` governs. `size_overrides` set for n ∈ {100, 500, 1k, 2k, 4k}: max_lr 3e-4→1e-3 (vs flat 3e-3), max_epochs 50→150 (vs flat 200). n ≥ 8000 keeps defaults unchanged. Pretrained encoder loaded from `ssl_53_simclr_grid_r2_step/size_d256_L8/checkpoints/step_290000.pt` (same as exp 48's in-flight setting). Same 16 train_sizes, val=1M, 40 evals, bf16, torch.compile, seed=42. Walltime 8h. New CSV column `max_lr` captures the resolved per-size LR for cross-size plotting. Code paths exercised by new tests `tests/test_ds_grid_overrides.py` (11 cases covering resolve_size_config + _validate_size_overrides; all pass locally). | [pending] | [pending] |
| 2026-04-24 | ssl_54_simclr_grid_yoran | Same architecture and training as ssl_53 but using the yoran dataset (instead of ob007, which is known to contain artifacts). Tests whether the artifact-free dataset changes per-size scaling and whether the larger models stop overfitting once exposed to more new data over the 48 h walltime. | 4 sizes (d128_L4, d256_L8, d512_L8, d768_L8) mirror ssl_53 exactly: SimCLRSmrt, ds_limit=0, epochs=50, AdamW max_lr=3e-4 wd=1e-4 pct_start=0.05, grad_clip=5.0, temperature=0.1, ChunkedRandomSampler chunk_size=2048, probe/checkpoint/resume cadences 10k steps. Only difference vs ssl_53: ssl_dataset='yoran_raw.memmap'. Walltime 30:00:00. | d=768 collapsed: embed_z_std drifted from ~0 to ~50 over first 100k steps with grad_norm bounded ~0.4 (clip never engaged), then catastrophically exploded around step 150k–200k — grad_norm spiked to ~6.5×10¹⁷ then went to Infinity for the remaining ~770k steps, embed_z_std jumped to ~3.6M and plateaued, probe_top1 collapsed from a 0.65 epoch-1 peak to 0.50 (random) and stayed there. d=512 stable: embed_z_std bumped to ~60 around step 100k then decayed to ~50, probe_top1 plateaued ~0.65 with mild upward trend. d=128, d=256 results TBD. | Width-dependent failure mode in `MLPProjectionHead`: the projection head's `d_model²` parameter scaling means d=768 has ~21× the capacity of d=128's head, enough to find a magnitude-runaway attractor that gradient damping through `F.normalize` (Jacobian scales 1/||z||) cannot escape. The runaway eventually produces non-finite gradients in a single batch; `clip_grad_norm_` cannot recover (`5/Inf=0`, `0×Inf=NaN`) and the model is corrupted permanently. Encoder is briefly capable at d=768 (epoch 1 probe = 0.65 = d=512's plateau), so this is a head-dynamics problem, not encoder capacity. Fix tested in ssl_55 (LayerNorm in projection head + non-finite-grad skip in training loop). See docs/negative_results.md for full analysis. |
| 2026-04-27 | ssl_55_simclr_grid_lnhead | Tests whether adding LayerNorm to the projection head (between Linear→GELU on hidden layers and a final LN before F.normalize in NTXent) and skipping the optimiser step when grad_norm is non-finite, together prevent the catastrophic d=768 failure observed in ssl_54. Pass criterion at d=768: embed_z_std stays bounded (predicted ~1–5 vs ssl_54's 3.6M); probe_top1 holds at minimum the ssl_54 epoch-1 peak of 0.65 instead of collapsing to 0.50; nonfinite_skip_count stays at 0 (if it fires, LN didn't fully fix the runaway). Single-variable architectural diff vs ssl_54 (training-loop skip is purely defensive — should not engage). | 4 sizes (d128_L4, d256_L8, d512_L8, d768_L8) mirror ssl_54 exactly except (a) `SimCLRSmrtLN` instead of `SimCLRSmrt` — uses `MLPProjectionHeadLN` (Linear→LN→GELU on hidden, final LN after last Linear), and (b) `_shared_train.py` skips both `optimizer.step()` and `scheduler.step()` when `clip_grad_norm_` returns a non-finite value, with a `nonfinite_skip_count` TB scalar logging cumulative skips. All hyperparameters identical to ssl_54: yoran dataset, ds_limit=0, epochs=50, AdamW max_lr=3e-4 wd=1e-4 pct_start=0.05, grad_clip=5.0, temperature=0.1, batch sizes 512/512/512/256 (d128/d256/d512/d768), ChunkedRandomSampler chunk_size=2048, probe/ckpt/resume every 10k steps. Walltime 30:00:00. New classes added to `smrt_foundation/model.py` under an `### Experimental (ssl_55)` section header; ssl_54 baseline classes (`SimCLRSmrt`, `MLPProjectionHead`) preserved unchanged for reproducibility. | LN fix bounded magnitude as designed: embed_z_norm at d=768 sits at ~14 (vs ssl_54's 3.6M plateau), d=512 at ~12 (vs ssl_54's 50). At d=512 grad_norm is rock-stable at 0.42 throughout, mirroring ssl_54. **But d=768 still fails differently:** probe_top1 peaks at 0.65 epoch 1, drops to 0.50 by step ~100k, and a finite-but-huge grad_norm spike (~10¹⁸, post-clip values still bounded) hits at step ~500k. Loss bounces 7.5–10.4. The non-finite skip didn't engage because grad values were huge but defined. **d=512 probe is unstable:** oscillates 0.61–0.67 with no monotone improvement — best accuracy is at the *start* of training. d=128, d=256 results TBD. | LN successfully addresses the magnitude-runaway pathway (the cause of ssl_54's catastrophic failure), but a separate failure mode remains. The d=512 "best-at-start" pattern is a textbook signature of **augmentation–task mismatch**: `random_subcrop` pairs arbitrary distant 32-base windows from the same long read as positives, so the encoder learns same-read membership via global polymerase/kinetic statistics rather than the local kinetic patterns where methylation lives. The CNN's locality bias gives the encoder useful CpG signal in epoch 1; the contrastive loss progressively erodes it. d=768's higher capacity erodes faster (full collapse). The ssl_54-era probe-decline pattern observed at d=768 is now diagnosed as the same family of failure as ssl_21's "probe declines over epochs" — different bad pairing, same family. ssl_56 tests the obvious cause-side fix: change the augmentation invariant from "any-position-within-read" to "short-range neighbor (gap_bp non-overlapping bases on the same molecule)". |
| 2026-04-28 | ssl_56_simclr_neighbor_invariance | Tests whether changing the SimCLR invariant from "any-position-within-read" (ssl_54/55's `random_subcrop` policy) to "short-range neighbor invariance" — two non-overlapping 32-base windows separated by `gap_bp` real bases on the same molecule — eliminates the probe oscillation and "best accuracy at the start" pattern that ssl_55's d=512 trace exhibited. Hypothesis: the `random_subcrop` policy lets the encoder discriminate via global read-level statistics (polymerase quality, average kinetic intensity), which actively erodes the local CpG-relevant features the methylation probe needs. Restricting positives to nearby windows aligns the contrastive task with the locality the methylation signal lives at. Pilot only — d=128_L4 anchor at four gap values; if a clear winner emerges with monotone-improving probe, follow up with a size grid. Pass: at least one of the four gap values produces a monotone-increasing probe_top1 trajectory (rather than oscillating with best-at-start). Fail: if all four oscillate, the augmentation locality hypothesis is falsified and focus pivots to masked-prediction (modernized Smrt2Vec). | New `NeighborPairAugmentationPolicy` (sibling of `AugmentationPolicy` in `smrt_foundation/augment.py`, marked `### Experimental (ssl_56)`) sweeps `gap_bp ∈ {16, 32, 64, 128}` across four sub-experiments (`gap_16/`, `gap_32/`, `gap_64/`, `gap_128/`). All other knobs identical to ssl_55_d128_L4: SimCLRSmrtLN encoder (LN projection head retained — orthogonal magnitude-runaway fix), non-finite-grad skip retained (defensive), yoran dataset, ds_limit=0, epochs=50, AdamW max_lr=3e-4 wd=1e-4 pct_start=0.05, grad_clip=5.0, temperature=0.1, ChunkedRandomSampler chunk_size=2048, probe/ckpt/resume every 10k steps. Per-view augmentations (channel_dropout_p=0.2, gaussian_noise_p=0.8 σ=0.1, blur_p=0.5 σ∈[0.2,2.0]) identical to ssl_55. **Resources:** 4 GPUs (half a node) per sub-experiment, batch_size=1024 per rank, preserving ssl_55_d128's effective negative count of 8190 via 2·4·1024−2 = 2·8·512−2. Walltime 30:00:00 per sub-experiment. Smoke-tested locally: `neighbor_pair_subcrop` correctness verified for gap ∈ {16,32,64,128}, short-read fallback returns all-pad views without crashing. | [pending] | [pending] |
| 2026-04-29 | ssl_57_inputmask_grid_lnhead | Carries ssl_21's input-masked-prediction objective (`Smrt2VecInputMask` + `AgInfoNCE`) into ssl_55's modernized harness, scaled across the same four-size grid ssl_55 swept. Two questions in one experiment: (1) does the masked-prediction objective scale monotonically with model capacity under the LN-head + non-finite-skip + ChunkedRandomSampler harness, or does it surface a different failure mode at d=768 than the contrastive head? (2) does the per-gap SSL pair-val (adapted from ssl_56 via encoder.forward + center-latent pooling — no projection) give a meaningful representation-quality diagnostic for an encoder that wasn't trained on pair retrieval? Pass at d=128: probe_top1 ≥ 0.58 at end (matches ssl_21's reported value) AND non-decreasing over the last 3 probe evals. Cross-size scaling slope reported separately. | New `Smrt2VecInputMaskLN` class added to `smrt_foundation/model.py` under `### EXPERIMENTAL (ssl_57)` section header (identical to `Smrt2VecInputMask` except project head is Linear→LN→GELU→Linear→LN — the load-bearing final LN bounds c_proj's per-channel scale before AgInfoNCE's F.normalize). Original `Smrt2VecInputMask` preserved unchanged for ssl_21 reproducibility. 4 sizes (d128_L4 / d256_L8 / d512_L8 / d768_L8) mirror ssl_55's axes (head_dim=64) but with ctx=4096, batch_size 64/32/16/8 (memory-feasible at long context). yoran dataset, ds_limit=0. AdamW max_lr=3e-4, wd=0.02 (matches ssl_21), pct_start=0.10 (compromise between ssl_21's 25% and ssl_55's 5%), grad_clip=5.0 with non-finite-skip. AgInfoNCE temperature=0.1, max_negatives=8192 (load-bearing at d=128, no-op at d=512+). Mask: p_mask=0.05 (sparser than ssl_21's 0.15 per user choice), mask_size=10. Step-cap schedule: total_steps=300000 explicit (decoupled from epochs), cosine over 270k post-warmup. ChunkedRandomSampler chunk_size=2048. Probe/ckpt/resume every 10k steps. SSL pair val: ssl_56's `ssl_pair_val_eval` adapted for this encoder — `encoder.forward(view)` + center-latent pooling (CpG-probe convention), no projection (project head was trained for per-position context retrieval, not pair retrieval). Reuses `ssl_pair_val_yoran_v1.memmap` (in-distribution) and `ssl_pair_val_ob007_v1.memmap` (held-out) verbatim from ssl_56. **Resources:** 8 GPUs, 32 cores, 256GB, 48h walltime per size (uniform; d=128 expected to finish 300k steps in ~12-15h, d=768 expected to run to walltime mid-cosine at ~150k steps — step-based ckpts mean partial runs still ship a portable encoder). Smoke-tested locally: param-count diff between Smrt2VecInputMaskLN and Smrt2VecInputMask = 512 (exactly the LN params, 4 LNs × 128); encoder state-dict keys identical to DirectClassifier.encoder; pair-val adapter produces 13 finite metrics on synthetic PairedGapMemmapDataset; AgInfoNCE forward+backward succeeds with 121/123 finite grads (the 2 missing are encoder.layer_norm_target.{weight,bias} which only sees the no_grad target path — same as ssl_21, handled by find_unused_parameters=True). | [pending] | [pending] |
| 2026-04-29 | ssl_57_inputmask_grid_lnhead_1m | At 300k steps the d=512 and d=768 probe_top1 trajectories had not plateaued (still climbing into the cosine floor); the d=128 trajectory looked plateaued but is below the 0.58 pass threshold. Rerun all four sizes at total_steps=1M on a matched cosine to get a valid cross-size scaling slope and to test whether the larger sizes' probe slopes continue. Fresh start, not a resume from the 300k checkpoints, so all four sizes share an identical schedule horizon. Hypothesis: probe_top1 keeps climbing for d=512/768; d=128 stays plateaued (capacity-limited); the d=128 vs d=768 gap widens at 1M relative to 300k. | New sibling sub-experiments `size_{d128_L4,d256_L8,d512_L8,d768_L8}_1m/` under ssl_57. Each config diff vs the original 300k size is exactly two lines: `experiment_name` appended `_1m`, `total_steps: 300000 → 1000000`. All other knobs unchanged: AdamW max_lr=3e-4 wd=0.02 pct_start=0.10 (now 100k warmup, 900k cosine), grad_clip=5.0 with non-finite-skip, AgInfoNCE temperature=0.1 max_negatives=8192, p_mask=0.05 mask_size=10, ChunkedRandomSampler chunk_size=2048, probe/ckpt/resume every 10k steps, yoran dataset, ds_limit=0, ctx=4096, per-size batch_size 64/32/16/8. The original `size_{...}/` directories and their 300k checkpoints are left untouched as historical artifacts backing the prior log row. Walltime 48h per size; observed throughput on the 300k runs (d=768 finished 300k in <12h, much faster than the README's naive-FLOPS-extrapolation estimate) gives high confidence d=128/d=256 will complete 1M well within walltime; d=512/d=768 likely complete or are very close at 48h, and the user's spare GPU-hours budget covers any resume cycles needed. The fresh-start choice trades 300k × 4 sizes of existing training for matched-schedule cleanness across sizes — the right trade for the cross-size comparison this row is designed to produce. | All four sizes collapsed during the high-LR phase. d=512 representative trace: train_loss 4 → ~10 around step 50k, embed_z_norm 22 → 14 in the same window, embed_z_std 0.95 → 0.4 with high oscillation, probe_top1 0.63 peak → 0.5 (chance). Pattern consistent across sizes. | Dimensional collapse on the LN projection head — channel-wise gammas dying, tracked by the z_std drop. Initially suspected warmup-duration as the trigger (failure at step ~50k where the 1m run is mid-warmup at lower absolute LR than the 300k run at the same step) → tested in 1m_v2. Subsequent investigation found `accelerator.prepare(scheduler)` compresses the schedule by `num_processes` (see `docs/negative_results.md` 2026-05-04); under the bug, the 1m run's effective cosine bottoms at global step ~125k, so the encoder was actually crossing past peak LR and into the cosine descent at the failure step, not still warming up. Patched and rerun as 1m_v3. |
| 2026-04-29 | supervised_50_finetune_grid_v2 | First end-to-end fine-tune comparison with architecture-matched random-init baselines for the d=256/d=512/d=768 archs that supervised_47 (d=128 only) didn't cover. Three questions on one schedule: (1) does masked-prediction pretraining (ssl_57_300k) beat supervised-from-scratch at each architecture? (2) does masked-prediction beat contrastive (ssl_55_300k) head-to-head at d=128, d=256, d=512? (3) how does the SSL-vs-baseline gap scale with architecture and labeled-data budget? Hypothesis: ssl_57 transfer beats random init at low n with the gap shrinking toward saturation at full data; ssl_57 vs ssl_55 lands within the probe-noise margin (~2 pp) so the value-add of masked-pred over contrastive will be visible only at small n if at all. The 300k ssl_57 / ssl_55 checkpoints are used here despite the ssl_57 1m extension being in flight; if the 1m result lands meaningfully different, the same matrix will be re-run pointing at the new checkpoints. | New `scripts/ds_grid_v2.py` (matrixed trainer, sibling of v1 ds_grid; reuses v1's `compute_step_budget`, `compute_batch_size`, `load_pretrained_encoder` (with its `pe.pe`-shape-mismatch filter), `preload_to_gpu`, `build_metrics`, `evaluate` by import). New experiment dir `scripts/experiments/supervised_50_finetune_grid_v2/` with one config defining the matrix: 4 architectures (d128_L4, d256_L8, d512_L8, d768_L8) × 3 inits (random, ssl_57_300k via `ssl_57_inputmask_grid_lnhead/size_{arch}/checkpoints/final_model.pt` template, ssl_55_300k via `ssl_55_simclr_grid_lnhead/size_{arch}/checkpoints/final_model.pt`) × 3 SimCLR-canonical train_sizes (10k / 100k / 8M) = 33 runs. ssl_55×d768 in skip list (d=768 SimCLR collapsed pre-LN-head fix). Single-stage AdamW max_lr=3e-3, weight_decay=0.02, pct_start=0.1, batch_size=4096 (mirrors supervised_46/47/48). `min_steps=100` (NOT supervised_47's 10000) lets max_epochs govern at small n, avoiding the exp_49 silent-overtraining failure. n_evals=40 log-spaced. Output: `experiment_dir/<init>/<arch>/n<size>/results.csv` per combo, merged top-level CSV with arch_name/init_name as leftmost columns for direct group-by plotting. Load balancer (`assign_combos`) uses FLOPS-weighted greedy: `step_budget × d_model^2 × n_layers`; with 8 GPUs the d=768/8M combos individually exceed per-GPU average so spread is ~85% (acceptable; flagged in README with mitigation options for future calibration). Verification: `DS_GRID_DRY_RUN=1` env var prints expansion + assignment preview without GPU dispatch; `DS_GRID_FORCE_WORLD_SIZE=8` simulates 8-GPU assignment for inspection from no-GPU host. Local dry run confirmed 33 combos, correct skip behavior, and matrix counts (random: 12, ssl_57: 12, ssl_55: 9). Resources: 32 cores, 256gb, 24h walltime, gres=gpu:8. | [pending] | [pending] |
| 2026-05-01 | ssl_57_inputmask_grid_lnhead_1m_v2 | The 1m runs (all four sizes, started 2026-04-29) collapsed early during the warmup phase: train_loss spiked from ~4 to ~10 around step 50k, embed_z_norm dropped from 22 to 14 over 50k steps (vs the 300k run's smooth 22 → 18 over 300k), embed_z_std collapsed from ~0.95 to ~0.4 with high oscillation, and probe_top1 crashed from 0.63 to chance (0.5). Diagnosis: dimensional collapse on the projection head (some channel-wise gammas dying, tracked by the z_std drop), most likely triggered by the longer pct_start=0.10 warmup integrating too many gradient updates against AgInfoNCE's moving-target dynamics (CNN latents from the same network) before peak LR is reached. Counterintuitively, the failure step (~50k of 1m run) was at lower absolute LR (~1.5e-4 mid-warmup) than the same step in the 300k run (~2.99e-4 just past peak), so the issue is warmup duration not LR magnitude — see image-zoomed early trajectory comparison and proposed shared-attractor hypothesis (the 300k run was descending toward the same collapsed state, just saved by cosine bottoming out before crossing the tipping point). Hypothesis under test: shortening pct_start from 0.10 to 0.03 (so warmup is 30k steps regardless of total_steps, matching the working 300k run's warmup duration) prevents the collapse. Pass: probe_top1 trajectory monotone-improving past step 100k for at least one size; train_loss stays bounded; embed_z_std stays > 0.7. Fail: collapse still happens, just later — that result rules out the warmup-only hypothesis and the next intervention becomes one of the anti-collapse mechanisms (EMA teacher / DINO centering / VICReg variance regularization / SimSiam stop-gradient). | New sibling sub-experiments `size_{d128_L4,d256_L8,d512_L8,d768_L8}_1m_v2/` under `ssl_57_inputmask_grid_lnhead/`. Each config diff vs the corresponding `size_*_1m/` config is exactly two lines: `experiment_name` appended `_v2`, `pct_start: 0.10 → 0.03`. Everything else identical: total_steps=1M unchanged (so cosine descent is still 970k steps, much gentler than the 300k run's 270k descent — flag for reading the result), max_lr=3e-4, weight_decay=0.02, grad_clip=5.0, AgInfoNCE temperature=0.1 max_negatives=8192, p_mask=0.05 mask_size=10, ChunkedRandomSampler chunk_size=2048, probe/ckpt/resume every 10k steps, yoran dataset, ds_limit=0, ctx=4096, per-size batch_size 64/32/16/8. Fresh start, no resume from the failed 1m checkpoints (those are useless — projection head is collapsed). The `size_*_1m/` directories from the failed runs are left untouched as historical artifacts backing this row's diagnosis. Walltime 48h/size. Reading caveat: 1m_v2 spends much more wall-time at high LR than the 300k run did (cosine 970k vs 270k steps) — if collapse re-occurs at step ~200-500k instead of step ~50k, that's NOT "almost-worked", that's "warmup duration alone wasn't the trigger" and points to cumulative high-LR exposure as the underlying mechanism. Plot at `report/training_dynamics/ssl57_zstd_attractor/` produces the z_std-vs-cumulative-LR overlay that disambiguates this. | Same failure mode as 1m_v1, shifted in time. Zoomed early embed_z_std trace shows v2 and the 300k run tracking together through ~25k steps, then v2 diverging faster than 1m_v1 did. Probe collapses to chance, z_std oscillates around 0.4. | Warmup-duration hypothesis falsified: collapse mechanism survives `pct_start=0.03`. The shared early-trajectory shape between v2 and the 300k run motivated the `report/training_dynamics/ssl57_zstd_attractor/` plot (z_std vs cumulative LR) to test whether both runs are on the same attractor with the 300k run saved by cosine bottoming early. Inspecting the actual LR schedule that motivated then surfaced the AcceleratedScheduler step-multiplier bug (`docs/negative_results.md` 2026-05-04), which compresses the schedule by 8× and explains why every prior "longer training" hypothesis got flipped on its head. Patched and rerun as 1m_v3. |
| 2026-05-04 | ssl_57_inputmask_grid_lnhead_1m_v3 | The 1m_v2 collapse (later than 1m_v1 but still during high-LR phase) prompted closer inspection of the actual LR schedule. Found a pre-existing bug in `ssl_57_inputmask_grid_lnhead/_shared_train.py:463`: `scheduler = accelerator.prepare(scheduler)` wraps the LambdaLR as `AcceleratedScheduler`, whose `step()` method advances the underlying LambdaLR by `num_processes` per call (default `split_batches=False` path; verified directly against `accelerate==1.13.0` source — see `accelerate.scheduler.AcceleratedScheduler.step` body). With 8 GPUs and `total_steps=1000000`, the cosine bottoms at global step **125,000** (= 1M / 8), and the remaining ~87% of training runs at `min_lr_ratio × max_lr = 1.5e-5` — effectively frozen. **This bug is present in every Accelerate-driven SSL run in the project: ssl_53/54/55/56/57.** The supervised harness already had this fix in supervised_40 (LR-scheduler-bug entry); the fix never propagated to SSL. Caveats this introduces for past SSL probe trajectories: the 300k runs trained at full LR for ~37k global steps then ran at 1.5e-5 for the remaining 263k; the "stable plateau" of probe_top1 in the late 300k phase was the model not actually training. ssl_55's "oscillating without monotone improvement" at d=512, ssl_56's flat probe — same dynamic. Hypothesis under v3: with the schedule actually spanning 1M global steps, the encoder gets a long enough cosine descent to learn through the unstable region (and either cross past it or collapse far enough into training that we can read the dynamics cleanly). Pass at any size: probe_top1 monotone-ish improving past step 200k AND embed_z_std stays > 0.7 AND train_loss bounded < 6 throughout. Fail: collapse pattern matches v1/v2, and the next single-variable test is `max_lr: 3e-4 → 1e-4` (the canonical SSL recipe per BERT/MAE/Wav2Vec). | One-line patch to `ssl_57_inputmask_grid_lnhead/_shared_train.py:461-463`: removed `scheduler = accelerator.prepare(scheduler)`, replaced with a comment explaining the AcceleratedScheduler step-multiplier bug and pointing at supervised_40 as the precedent fix. The raw `LambdaLR` from `get_cosine_schedule_with_warmup` (`smrt_foundation/optim.py:5`) is now stepped once per global step in the training loop, as intended. New sibling sub-experiments `size_{d128_L4,d256_L8,d512_L8,d768_L8}_1m_v3/` under ssl_57. Each config differs from the corresponding `_1m_v2/config.yaml` by exactly one line: `experiment_name` swapped from `..._1m_v2` to `..._1m_v3`. **No other config change** — `pct_start: 0.03` and `max_lr: 3e-4` carry over from v2; `total_steps: 1M` unchanged. With the patch, `pct_start: 0.03` now produces a true 30k-step warmup, then cosine over 970k steps to a 1.5e-5 floor. The `size_*_1m/` and `size_*_1m_v2/` directories are left untouched as historical artifacts backing this row's diagnosis. The same bug exists in `ssl_55_simclr_grid_lnhead/_shared_train.py` and `ssl_56_simclr_neighbor_invariance/_shared_train.py` (also Accelerate-prepared schedulers); those were not patched here because ssl_55/56 are not being rerun, but if either is ever resubmitted the same one-line removal is needed. Resources unchanged from v2: 32 cores, 256gb, 48h walltime, gres=gpu:8. | [pending] | [pending] |
