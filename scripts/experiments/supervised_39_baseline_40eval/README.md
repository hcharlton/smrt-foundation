# Experiment 39: Baseline Data Scaling (40 eval points)

Identical to exp 33 (random init, single-stage AdamW lr=3e-3) but with 40 log-spaced eval points instead of 20. Matches exp 38's eval density for fair comparison.

## Submission

```bash
bash run.sh scripts/experiments/supervised_39_baseline_40eval
```
